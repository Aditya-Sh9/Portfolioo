import { SOLACE } from "@/data/projects";

<CaseSection index="01" title="Overview">

SOLACE is an emotionally intelligent AI wellness companion. It’s not a medical app and it’s not a chatbot — it’s built to feel like a personal health detective with a warm, human soul: something that helps you notice the quiet, hard-to-name things (low energy, brain fog, mood swings, “just off”) and surfaces honest, non-clinical patterns in your own data, without ever diagnosing or prescribing.

It’s a solo portfolio project, built in public phases, and it’s in active development. Phase 4 of 7 is complete.

</CaseSection>

<CaseSection index="02" title="What it does">

- **Daily check-ins** — mood, energy, sleep, water, sunlight, stress, symptoms, food — in under two minutes.
- **A rule engine** that maps symptom patterns to possible nutrient and lifestyle flags (iron, vitamin D, magnesium, B12, vitamin C, sleep debt, dehydration, sedentary behavior, sustained stress), sourced from peer-reviewed nutrition research.
- **Gemini-powered insight cards** that turn those flags into warm, specific, hedged language — never “you have a deficiency,” always “there might be a pattern here.”
- **A personal pattern model** (scikit-learn, trained per-user, stateless) that learns your data after 14+ check-ins and surfaces an honest next-day mood/energy prediction — banded into plain language (“steadier,” “brighter”) rather than raw numbers, and hidden entirely when the model isn’t confident.

Coming next: a fully client-side encrypted journal the app itself can never read, and a cycle-aware wellness section.

</CaseSection>

<CaseSection index="03" title="What it deliberately doesn’t do">

No diagnoses. No medical claims. No supplement or affiliate pushing. No dark patterns.

The journal (once built) will never feed any AI/ML layer — that boundary is architectural, not a setting.

</CaseSection>

<CaseSection index="04" title="Architecture">

Three independently deployed services:

<ArchitectureDiagram
  core="api"
  nodes={[
    { id: "client", label: "Next.js", detail: "Vercel", layer: "client" },
    { id: "api", label: "Express + TypeScript API", detail: "Railway", layer: "core" },
    { id: "db", label: "Supabase Postgres", detail: "Postgres + Auth, via Prisma", layer: "data" },
    { id: "gemini", label: "Google Gemini Flash", layer: "external" },
    { id: "ml", label: "FastAPI ML service", detail: "Railway, stateless", layer: "service" },
  ]}
  edges={[
    { from: "client", to: "api", kind: "sync" },
    { from: "api", to: "db", label: "Prisma", kind: "sync" },
    { from: "api", to: "gemini", label: "insight cards", kind: "sync" },
    { from: "api", to: "ml", label: "GET /api/patterns, decoupled", kind: "async" },
  ]}
/>

- The frontend never talks to the database, Gemini, or the ML service directly — everything routes through the backend.
- The ML service is stateless. There are no per-user model files: the backend sends a user’s check-in history with each call and the service trains a small gradient-boosted model in memory, in milliseconds.
- `GET /api/patterns` is decoupled from the main dashboard load, so a cold ML service never blocks the rest of the app.
- Journal encryption (Phase 5) will happen entirely in the browser via the Web Crypto API. The server will only ever store ciphertext.

<Note>The backend and ML service run on Railway’s free tier, which sleeps after inactivity — the first request after a while may take a few seconds to wake up.</Note>

</CaseSection>

<CaseSection index="05" title="Stack">

<Spec
  items={[
    { label: "Frontend", value: "Next.js 16 (App Router), TypeScript, Tailwind CSS 4, Framer Motion, custom hand-drawn UI kit" },
    { label: "Backend", value: "Express 5, TypeScript, Prisma, Zod" },
    { label: "ML service", value: "Python 3.12, FastAPI, scikit-learn, pandas, numpy, scipy" },
    { label: "Auth & DB", value: "Supabase (Postgres + Auth)" },
    { label: "AI", value: "Google Gemini Flash" },
    { label: "Deploy", value: "Vercel (frontend), Railway (backend + ML), Supabase Cloud" },
  ]}
/>

</CaseSection>

<CaseSection index="06" title="Progress">

<PhaseList phases={SOLACE.phases} />

An emotion-classification layer (reading tone from check-in notes) is intentionally deferred to Phase 7 — free-tier serverless inference wasn’t reliable enough to ship earlier.

</CaseSection>
